<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.miniproject.mapper.SaleMapper">

    <insert id="shopSave" parameterType="com.spring.miniproject.domain.Sale">

        insert into shopping values(pnumber=#{pnumber}, pname=#{pname}, price=#{price}, image=#{image})

    </insert>

    <update id="shopModify" parameterType="com.spring.miniproject.domain.Sale">

        update shopping set pname=#{pname}, price=#{price}, image=#{image} where pnumber=#{pnumber}

    </update>

</mapper>